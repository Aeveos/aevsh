#pragma once

extern char input[256];
extern int num;
extern char history[10][256];
extern int history_count;

 // 1 for in quotes and 0 for outside quotes.
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <process.h>